# Supabase
SUPABASE_URL=https://your-supabase-url.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here

# SMTP (SendGrid example)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=YOUR_SENDGRID_API_KEY
SMTP_FROM="Pluvia Academy <no-reply@yourdomain.com>"

# Optional: Brevo / Sendinblue API key (preferred over SMTP)
# Get it from Brevo dashboard -> SMTP & API -> API Keys
# Example: BREVO_API_KEY=xkeysib_xxx
BREVO_API_KEY=

# Optional: test recipient used by /api/auth/test-email when no ?to= specified
SMTP_TEST_TO=you@example.com

# Optional: Twilio (for SMS fallback)
# TWILIO_SID=your_twilio_sid
# TWILIO_TOKEN=your_twilio_token
# TWILIO_FROM=+1234567890

# Other optional settings
# OTP_MAX_ATTEMPTS=5
# SESSION_DAYS=30
# COOKIE_SECURE=0

# OTP template customization (optional)
# Use these to control subject and body for verification emails. Replace {{code}} and {{minutes}}.
OTP_MINUTES=10
OTP_SUBJECT=Pluvia Academy - Kode Verifikasi
OTP_TEXT=Kode verifikasi Anda: {{code}} (berlaku {{minutes}} menit)
OTP_HTML=<p>Kode verifikasi Anda: <strong>{{code}}</strong></p><p>Berlaku {{minutes}} menit.</p>
